{
    "name": "translation-app-monorerepo",
    "private": true,
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "f-install": "cd frontend && npm install",
        "b-install": "cd backend && npm install"
    }
}